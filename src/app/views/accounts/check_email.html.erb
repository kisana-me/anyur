<% content_for :title, "認証コードを入力してメールを変更" %>

<h1>認証コードを入力してメールを変更</h1>
<p>新しいメールアドレスに送信された認証コードを入力してください。</p>

<%= form_with(model: @current_account, url: account_email_update_path, method: :patch, class: "ctmf", data: { turbo: false }) do |form| %>
  <%= render "shared/error_messages", instead: @current_account %>

  <div>
    <%= form.label :authentication_code %>
    <%= form.text_field :authentication_code, pattern: "\\d{6}", required: true, maxlength: 6, autofocus: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
