<% content_for :title, "メールアドレスを変更" %>

<h1>メールアドレスを変更</h1>
<p>現在のパスワードと、新しいメールアドレスを入力してください。</p>

<p>次の画面で新しいメールアドレスに認証コードを送信します。</p>

<%= form_with(model: @current_account, url: account_email_check_path, method: :post, class: "ctmf", data: { turbo: false }) do |form| %>
  <%= render "shared/error_messages", instead: @current_account %>

  <div>
    <%= form.label :current_password %>
    <%= form.password_field :current_password, value: params.dig("account", "current_password"), data: { controller: "ctmf-password" } %>
  </div>

  <div>
    <%= form.label :next_email %>
    <%= form.email_field :next_email, value: params.dig("account", "next_email") %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
