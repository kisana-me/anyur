<% content_for :title, "メールを変更" %>

<h1>メールを変更</h1>
<p>現在のパスワードと、新しいメールを入力してください。</p>
<% if @current_account.email_verified %>
<p>新しいメールに認証コードを送信して確認に進みます。</p>
<% end %>
<%= form_with(model: @ep_form, url: account_email_check_path, data: { turbo: false }) do |form| %>
  <%= render "shared/error_messages", instead: @ep_form %>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.password_field :password, value: @ep_form.password %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.email_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
